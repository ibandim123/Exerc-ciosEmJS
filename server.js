const express = require('express');//call express 

const app = express() // execute function express

app.get('/',(req,res) => {}) // route , req is "requisição" and res is "resposta"

app.listen(3001) // the gate ear